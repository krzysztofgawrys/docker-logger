import React from 'react';

import {Layout} from 'antd';

const {Footer} = Layout;

const footer = () => {
    return (
        <Footer style={{textAlign: 'center'}}>
            Dockers logger @ dmarczydlo
        </Footer>
    );
};

export default footer;
